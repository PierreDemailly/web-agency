{% extends 'base.html.twig' %}

{% block title %}Client index{% endblock %}

{% block body %}
<<<<<<< HEAD
<div id="content">

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid p-0">

            <button type="button" id="sidebarCollapse" class="navbar-btn">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <button class="btn btn-dark d-inline-block d-lg-none ml-2" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <i class="fas fa-align-justify"></i>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">

                <div class="breadcrumb my-2">
                    <a class="active" href="{{ path('client_index') }}">
                        <span class="breadcrumb__inner">
                            <span class="breadcrumb__title">Clients</span>
                            <span class="breadcrumb__desc">Accueil</span>
                        </span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="container">
 
        <h1 class="c-info">Clients enregistrés</h1>

        <a class="button button-secondary" href="{{ path('client_new') }}">Ajouter un client</a>

        <div class="row m-3">
            {% if clients is not empty %}
                <table class="table table-primary anim">
                    <thead>
                        <tr>
                            <th>Prénom</th>
                            <th>Nom</th>
                            <th class="d-none d-xl-inline-block">Identifiant</th>
                            <th class="d-none d-md-inline-block">Emprunts en cours</th>
                            <th class="center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for client in clients %}
                        <tr>
                            <td>{{ client.firstname }}</td>
                            <td>{{ client.lastname }}</td>
                            <td class="d-none d-xl-inline-block">{{ client.identifiant }}</td>
                            <td class="pl-3 d-none d-md-inline-block">{{ client.books | length }}</td>
                            <td class="center">
                                <a href="{{ path('client_show', {'id': client.id}) }}" class="button button-primary">Voir</a>
                                <a href="{{ path('client_edit', {'id': client.id}) }}" class="button button-secondary">Modifier</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% else %}
                <div class="alert alert-info">Aucun client trouvé.</div>
            {% endif %}
        </div>

    </div>

</div>

=======
    <h1>Client index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Token</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td>{{ client.id }}</td>
                <td>{{ client.firstname }}</td>
                <td>{{ client.lastname }}</td>
                <td>{{ client.token }}</td>
                <td>
                    <a href="{{ path('client_show', {'id': client.id}) }}">show</a>
                    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('client_new') }}">Create new</a>
>>>>>>> parent of 5d9b8a8... Merge pull request #5 from PierreDemailly/dev
{% endblock %}
