<div class="wrapper">
    <nav id="sidebar">
        <div class="sidebar-header">
            <h3>Ma Bibliothèque</h3>
        </div>

        <ul class="list-unstyled components">
            <p class="py-0">{{ app.user.firstname }} <small>{{ app.user.email }}</small></p>
            <div class=" py-0 pl-2">
                <a href="{{ path('logout') }}" class="button button-danger">Se déconnecter</a>
                </div>
            <li class="mt-3">
                <span class="list-head d-inline-block mb-0 ml-2">Livres</span>
                <ul class="list-unstyled">
                    <li>
                        <a href="{{ path('book_index') }}">Voir les livres</a>
                    </li>
                    <li>
                        <a href="{{ path('book_new') }}">Ajouter un livre</a>
                    </li>
                </ul>
            </li>
            <li>
                <span class="list-head d-inline-block mt-3 mb-0 ml-2">Clients</span>
                <ul class="list-unstyled">
                    <li>
                        <a href="{{ path('client_index') }}">Voir les clients</a>
                    </li>
                    <li>
                        <a href="{{ path('client_new') }}">Ajouter un client</a>
                    </li>
                </ul>
            </li>
        </ul>
    </nav>
